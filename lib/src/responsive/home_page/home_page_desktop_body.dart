import 'package:flutter/material.dart';

class MyHomePageDesktopBody extends StatelessWidget {
  const MyHomePageDesktopBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Desktop'),
      ),
    );
  }
}
